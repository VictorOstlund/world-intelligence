## Task wi-6ly — Task 1: Project scaffold, SQLite schema, and auth

- What was done: Scaffolded Next.js 15 + TypeScript project in `projects/world-intelligence/`. Implemented SQLite schema (reports, config, users, reports_fts FTS5 table). Auth system with bcrypt+JWT (jose), httpOnly cookies, session middleware. API routes: /api/auth/login, /api/auth/logout, /api/auth/me. Login page UI. DB helpers: getUser, createUser, getConfig, saveConfig, seedIfEmpty. .env.example. scripts/start.js production launcher. vitest config.
- What was learned: bd auto-closes epics when last child task closes — need to reopen manually when more tasks remain. All 13 tests pass (schema assertions, bcrypt round-trip, JWT sign/verify, login API integration). Task was already partially implemented at iteration start.
- Codebase state: `projects/world-intelligence/` has: lib/db.ts, lib/auth.ts, middleware.ts, app/api/auth/{login,logout,me}/route.ts, app/login/page.tsx, scripts/start.js, .env.example, vitest.config.ts. All tests green.

## Task wi-qye — Task 2: RSS feed loader from worldmonitor GitHub + per-category fetcher

- What was done: Created lib/feeds.ts (parseRssFeed pure function handles RSS 2.0 + Atom, fetchCategory for parallel multi-feed fetching, fetchFullArticle for full article text, loadFeedList with 1h TTL cache and hardcoded fallback feeds for all 15 categories). Created lib/categories.ts (DEFAULT_CATEGORIES with 15 categories matching worldmonitor spec, getCategoryConfig merging DB overrides with defaults, handles JSON string from SQLite). Added getActiveCategoryConfig() to lib/db.ts. 24 new tests written (12 parser tests, 12 category config tests). All 37 tests pass.
- What was learned: worldmonitor GitHub repo (github.com/worldmonitor/worldmonitor) returns 404 — doesn't actually exist publicly. loadFeedList gracefully falls back to hardcoded public RSS feeds. bd auto-closes the epic when last child task is closed — must manually reopen with `bd update wi-uhh --status open`. fast-xml-parser handles both RSS 2.0 and Atom format. Need to handle both `link` (string) and `link[@href]` (Atom attribute) cases.
- Codebase state: projects/world-intelligence/ now has lib/feeds.ts, lib/categories.ts (new). lib/db.ts has getActiveCategoryConfig(). src/__tests__/feeds/ has parser.test.ts and categories.test.ts. All 37 tests green.

## Task wi-ahc — Task 3: Two-tier pipeline — triage agent, synthesis, scheduler, and cost tracking

- What was done: Created lib/pipeline.ts (triageCategory with JSON parsing + high-importance full article fetch, synthesizeReport with body/summary/cost extraction). Created lib/scheduler.ts (node-cron singleton startScheduler/stopScheduler). Added Report CRUD to lib/db.ts: saveReport (INSERT + FTS update), getReport, getReports (paginated, newest-first), searchReports (FTS5 MATCH). All 62 tests pass.
- What was learned: FTS5 in SQLite requires explicit INSERT into reports_fts after inserting into reports (content table doesn't auto-sync on INSERT OR REPLACE — need to also insert into FTS). node-cron v4 has slightly different API but is compatible. bd auto-closes epics when last child closes — must reopen manually with `bd update wi-uhh --status open`. `BD` variable assignment with && chaining doesn't work in bash — use direct binary path.
- Codebase state: lib/ now has: auth.ts, categories.ts, db.ts (with Report CRUD), feeds.ts, llm.ts, pipeline.ts (triageCategory, synthesizeReport), scheduler.ts (startScheduler, stopScheduler). Tests: 62 passing. Frontend: only login page + placeholder home page. No reports/settings UI yet. No API routes except auth. Pipeline cannot be triggered from UI yet.

## Task wi-6vo — Task 5: A1 path (OpenClaw cron), .env wiring, and end-to-end validation

- What was done: Created scripts/a1-orchestrator.ts (spawnable by OpenClaw cron — calls runPipeline(), writes markdown report to /home/node/.openclaw/workspace/reports/, logs outcome to workspace memory). Added SCHEDULE_HOURS to .env.example. Wired seedIfEmpty() call into instrumentation.ts on server startup. Wrote 3 smoke tests in src/__tests__/api/pipeline.test.ts: pipeline/run saves report to DB and returns reportId; saved report appears in GET /api/reports list; disabled categories produce no fetchCategory calls. All 76 tests pass.
- What was learned: Default category config has 13 enabled categories — smoke tests that set up 3 callLLM mockResolvedValueOnce calls fail if all categories are enabled. Fix: explicitly configure category_config in test to disable all except the test categories. Using mockImplementation with prompt content check (triage prompt contains "triage agent") cleanly handles variable number of triage calls. bd auto-closes epics when last child closes — must reopen with `bd update wi-uhh --status open`.
- Codebase state: scripts/ now has a1-orchestrator.ts (tsx script). instrumentation.ts calls seedIfEmpty() on startup. .env.example has SCHEDULE_HOURS. src/__tests__/api/pipeline.test.ts is the new smoke test. 76 tests green. Remaining epic criteria: fallback chain test, auth middleware test, ≥5-category multi-category validation.

## Task wi-lr9 — Task 6: Fallback chain test, auth middleware test, and end-to-end multi-category validation

- What was done: Added non-retryable error test to `src/__tests__/pipeline/llm.test.ts` (400 is NOT retried, mockCreate called once). Created new `src/__tests__/api/middleware.test.ts` with 4 tests: redirect on no cookie, pass-through for /api/auth/*, allow valid session, redirect on invalid JWT. Added 5-category smoke test to `src/__tests__/api/pipeline.test.ts` — saves config with 5 enabled categories, mocks fetchCategory per cat, verifies ≥5 fetchCategory calls and itemCount > 0. All 82 tests pass.
- What was learned: URL mismatch is a subtle bug in smoke tests — `triagedJson` has fixed URL `https://example.com/1` but per-category fetchCategory mocks must return that SAME URL for triage score lookup to match. `bd` binary lives at `/home/node/.openclaw/workspaces/custom-agent/node_modules/@beads/bd/bin/bd` — not in PATH, must use absolute path. Next.js middleware tests work in vitest by importing `../../../middleware` directly and creating `new NextRequest(url)`.
- Codebase state: `src/__tests__/` now has 12 test files, 82 tests total. All epic success criteria validated: fallback chain (429/5xx retry, 400 no-retry, all-exhausted), auth middleware blocking, 5-category multi-category pipeline. Previous tasks implemented: scaffold+auth (wi-6ly), RSS feeds (wi-qye), pipeline+scheduler (wi-ahc), API routes+UI (wi-axr), A1 orchestrator (wi-6vo). Epic complete.

## Task wi-qi8 — Task 1: Swap driver and rewrite lib/db.ts for Postgres

- What was done: lib/db.ts was already rewritten with @neondatabase/serverless (Postgres), Neon driver, async query helper, all Postgres SQL (positional params, INSERT...ON CONFLICT, tsvector FTS, GIN index, trigger). Fixed 5 test files that failed due to vi.mock hoisting — `vi.mock` factories are hoisted above top-level variable declarations, so `mockSql` from `createMockSql()` was undefined. Fix: changed `vi.mock` → `vi.doMock` (not hoisted) in auth.test.ts, reports.test.ts, settings.test.ts, pipeline.test.ts, db/reports.test.ts. Created in-memory mock-neon.ts helper that simulates Postgres SQL patterns in-memory.
- What was learned: vitest `vi.mock` factory is hoisted to the top of the file, so it runs BEFORE any top-level variable declarations like `const mockSql = createMockSql(store)`. Use `vi.doMock` instead — it is NOT hoisted, so it runs in normal flow after variables are defined. Works correctly as long as the mocked module is dynamically imported (via `await import(...)`) AFTER the `vi.doMock` call, which all test files already do. bd auto-closes epics when last child closes — reopened with `bd update wi-ijs --status open`. better-sqlite3 only reference remaining is in pnpm-approved.yaml (harmless).
- Codebase state: lib/db.ts uses @neondatabase/serverless exclusively. package.json has @neondatabase/serverless (no better-sqlite3). All 83 tests pass (12 test files). npm run build succeeds. Schema: users, config, reports tables + tsvector search_vector + GIN index + trigger. Remaining: scripts/provision-neon.ts for Vercel deployment.

## Task wi-e7j — Task 2: Neon provisioning script for finish stage

- What was done: Created scripts/provision-neon.ts using Vercel REST API (no CLI dependency). Script reads .vercel/project.json for projectId/orgId, sets DATABASE_URL as encrypted env var (creates new or patches existing) for production+preview, then triggers a production redeploy (with fallback to redeploying last deployment). Exported `provision()` function for programmatic use. Added 6 tests in src/__tests__/api/provision.test.ts covering: import validation, missing DATABASE_URL, missing VERCEL_TOKEN, create new env var + redeploy, update existing env var, and fallback redeploy path. Updated .env.example with DATABASE_URL placeholder. All 89 tests pass, npm run build succeeds.
- What was learned: Vercel CLI is not installed in the container, so Vercel REST API v9 (env vars) and v13 (deployments) are the correct approach. .vercel/project.json already existed with projectId and orgId. VERCEL_TOKEN env var is available in the container. The deployment API requires a fallback strategy — creating a fresh deployment from gitSource may fail, so listing last deployment and redeploying it is more reliable.
- Codebase state: scripts/provision-neon.ts exists and is runnable with tsx. 13 test files, 89 tests total. .env.example has DATABASE_URL. Epic wi-ijs is fully complete from a code perspective. Remaining success criteria (live Vercel login, reports API on Vercel) are deployment-time validations for when the finish stage runs with a real DATABASE_URL.

## Task wi-eeg.1 — Task 1: API key sentinel masking and display
- What was done: Changed API key redaction from '*****' to '__masked__' sentinel. GET /api/settings returns '__masked__' for saved keys, '' for unconfigured. POST handler detects sentinel and preserves existing DB key instead of overwriting. UI detects sentinel on load, shows placeholder "API key saved (enter new to change)", sends sentinel back if user doesn't type new value. Fixed mock-neon.ts missing .query property (rawQuery calls getSql().query()). Fixed same issue in schema.test.ts inline mock.
- What was learned: rawQuery in db.ts uses getSql().query() — all mocks need a .query property on the mock function. The existing '*****' redaction was cosmetic only — no POST-side logic existed to prevent overwriting with the redacted value. The sentinel pattern cleanly separates "key exists" from "key value" without ever exposing real keys to the browser.
- Codebase state: app/api/settings/route.ts has MASKED_KEY_SENTINEL constant, GET returns '__masked__', POST preserves keys when sentinel received. app/settings/page.tsx tracks savedKeys state, shows placeholder for saved providers. 13 test files, 93 tests, all green. npm run build clean. Remaining epic criteria: markdown rendering (R2), report sections (R3), model dropdowns (R4), deduplication (R5).

## Task wi-41y — Task 2: Markdown rendering in report viewer
- What was done: Installed react-markdown + remark-gfm + @tailwindcss/typography. Replaced `<pre>{report.body}</pre>` with `<ReactMarkdown remarkPlugins={[remarkGfm]}>{report.body}</ReactMarkdown>` in app/reports/[id]/page.tsx. Added Tailwind prose classes for clean typography rendering. Added `@plugin "@tailwindcss/typography"` to globals.css. Wrote 6 tests in src/__tests__/ui/markdown-rendering.test.ts verifying headings, bold, bullet lists, tables, code blocks, and a full report body all render as proper HTML.
- What was learned: NODE_ENV=production in the container causes npm to skip devDependencies — must use `NODE_ENV=development npm ci` to get vitest installed. Tailwind v4 uses `@plugin` directive instead of plugins array in config. react-markdown works cleanly with renderToString for server-side unit testing. bd auto-closes epics when last child closes — reopened with `bd update wi-eeg --status open`.
- Codebase state: app/reports/[id]/page.tsx uses ReactMarkdown + remarkGfm. app/globals.css has @tailwindcss/typography plugin. 14 test files, 99 tests, all green. npm run build clean. Remaining epic criteria: report sections/synthesis prompt (R3), model dropdowns (R4), deduplication (R5).

## Task wi-2a8 — Task 3: Synthesis and triage prompt upgrade for all 6 report sections
- What was done: Updated triageCategory prompt to request contrarian_signal boolean for each item (flags contrarian views, underweighted topics, notable silences). Updated synthesizeReport prompt with all 6 required sections (Executive Summary, Key Themes & Patterns, Critical Events, Opportunities, Contrarian Angles, Coverage Gaps), European high-yield credit analyst context, and report header metadata (timestamp, categories covered, sources count, items reviewed). Added contrarian_signal to ScoredItem interface and merge logic. Wrote 4 new tests: synthesis prompt contains all 6 sections, synthesis prompt includes European HY credit context, synthesis prompt requests report header metadata, triage prompt requests contrarian_signal.
- What was learned: The triage model flags contrarian_signal at triage time — cheaper than having the synthesis model identify contrarian items from scratch. Template literal prompt with runtime metadata (categories list, item count, contrarian items) provides better LLM output. bd auto-closes epics when last child closes — reopened with `bd update wi-eeg --status open`.
- Codebase state: lib/pipeline.ts has upgraded triage prompt (contrarian_signal) and synthesis prompt (6 sections + header + HY credit context). ScoredItem has contrarian_signal?: boolean. 14 test files, 103 tests, all green. npm run build clean. Remaining epic criteria: model dropdowns (R4), deduplication (R5).

## Task wi-id0 — Task 4: Model dropdowns with per-provider options
- What was done: Created lib/models.ts as single source of truth for provider model lists (PROVIDER_MODELS constant, getModelsForProvider, getModelsWithCustom helpers). Updated app/settings/page.tsx: replaced free-text inputs for triage_model, synthesis_model, and fallback models with <select> dropdowns via a ModelSelect component. Dropdowns filter options by active provider and show "custom: [value]" disabled option for saved values not in the list. Wrote 9 tests covering PROVIDER_MODELS structure, per-provider models, azure/openai parity, helper functions, and custom model handling.
- What was learned: The getModelsWithCustom pattern cleanly handles backward compatibility — saved model values not in the dropdown list appear as disabled "custom: X" options instead of silently disappearing. Keeping ModelSelect as a local component in page.tsx (not a separate file) is simpler since it's only used in one place. bd auto-closes epics when last child closes — reopened with `bd update wi-eeg --status open`.
- Codebase state: lib/models.ts exports PROVIDER_MODELS (4 providers), getModelsForProvider, getModelsWithCustom. app/settings/page.tsx uses ModelSelect for triage/synthesis/fallback model fields. 15 test files, 112 tests, all green. npm run build clean. Remaining epic criteria: deduplication (R5).

## Task wi-54m — Task 5: Article deduplication with seen_articles table
- What was done: DB functions (markArticlesSeen, filterSeenUrls) and seen_articles table were already implemented. Pipeline wiring (filterSeenUrls after triage, markArticlesSeen after save) was already in place. Added 2 pipeline-level integration tests: (1) second pipeline run deduplicates previously seen articles (itemCount drops to 0), (2) items with importanceScore >= 9 bypass dedup even if URL was seen before. 7 unit tests for dedup DB functions were already in place.
- What was learned: Most of the dedup implementation was done in a prior iteration — the task was partially complete. The critical missing piece was pipeline-level integration tests proving the end-to-end dedup flow works. Use unique URLs per test (e.g. /dedup-1, /breaking-9) to avoid cross-test interference. Testing dedup requires two pipeline runs within the same `it` block since beforeEach clears seen_articles.
- Codebase state: lib/db.ts has seen_articles table, markArticlesSeen, filterSeenUrls. lib/pipeline.ts wires dedup into runPipeline. mock-neon.ts supports seen_articles queries. 16 test files, 122 tests, all green. npm run build clean. ALL epic success criteria (R1-R5) are met. Epic wi-eeg is CLOSED.

## Task wi-tbw.1 — Task 1: Pipeline fixes — report repetition, intra-run dedup, source links, cost, prompt cleanup
- What was done: Added truncateToFirstReport() post-processing that strips duplicate report blocks when LLM repeats the full report. Added intra-run URL dedup via in-memory Set in runPipeline (before cross-run filterSeenUrls, no DB side effects). Rewrote synthesis prompt: numbered sections (## 1. through ## 6.), domain-neutral analyst persona (removed all HY credit/spread language), explicit [Source](url) citation instruction, stop instruction ("Write the complete report exactly once"), format compliance ("You MUST include all 6 sections"). Rewrote triage prompt: removed domain-specific framing, added pubDate to item block. Added formatPubDate() helper for readable UTC dates. Added 6 new model prices to PRICE_TABLE: gemini-3.1-flash-lite-preview, gemini-2.5-flash-lite, gemini-2.5-flash, gemini-3.1-pro, gemini-3.1-flash-lite, claude-haiku-3-5. Updated 2 existing tests that checked for HY credit framing (now check for domain-neutral analyst persona). Created 2 new test files: pipeline-fixes.test.ts (10 tests) and llm-cost.test.ts (6 tests). All 138 tests pass, npm run build clean.
- What was learned: When removing domain-specific framing from prompts, existing tests that assert on old prompt content must be updated (not deleted). The intra-run dedup Set must come BEFORE cross-run filterSeenUrls to avoid passing duplicate URLs to the DB query. formatPubDate uses toUTCString() which outputs "Wed, 04 Mar 2026 14:30:00 GMT" — replace " GMT" with " UTC" for consistency. bd auto-closes epics when last child closes — reopened with `bd update wi-tbw --status open`.
- Codebase state: lib/pipeline.ts has truncateToFirstReport(), formatPubDate(), intra-run dedup, numbered-section synthesis prompt, domain-neutral triage prompt. lib/llm.ts has 6 new model prices. 18 test files, 138 tests, all green. npm run build clean. Remaining epic criteria: R5 (PDF download), R6 (UI overhaul), R7 (delete reports).

## Task wi-5vd — Task 2: PDF download button and delete reports
- What was done: Added window.print() "Download PDF" button to report viewer. Added print CSS in globals.css (hides nav/buttons, serif font, A4 page margins, page break rules). Added DELETE /api/reports/[id] route (deletes from reports + seen_articles). Added trash icon delete button on report cards in list. Added delete button in report viewer header. Confirm dialog on both. Redirect to /reports after deletion. Created 7 tests for delete API.
- What was learned: Task was partially implemented from prior work — the delete API route and button were already scaffolded. Print CSS requires hiding elements with class `no-print` and using `@page { size: A4; margin: 2cm }`. Browser's built-in Save as PDF works well with proper print styles.
- Codebase state: app/api/reports/[id]/route.ts has DELETE handler. app/reports/[id]/page.tsx has Download PDF button + Delete button. app/reports/page.tsx has trash icon per card. app/globals.css has @media print styles. 19 test files, 145 tests, all green. npm run build clean.

## Task wi-hha — Task 3: UI holistic overhaul — dark theme, layout, responsive design
- What was done: Complete UI redesign implementing R6. Created CSS custom properties for dark/light themes in globals.css (dark default: bg #0f1117, surface #1a1d27, border #2d3148, text #e8eaf6, secondary #8892b0, accent #4f8ef7; light mode via [data-theme="light"]). Registered all tokens in Tailwind @theme inline block. Created NavBar.tsx client component with: app name link, Reports/Settings nav links with active state highlighting, dark/light theme toggle (sun/moon icons), mobile hamburger menu. Layout.tsx: inline script to prevent flash of wrong theme (reads localStorage before render), NavBar rendered globally, metadata updated. Reports list (/reports): responsive card grid (sm:2, lg:3 cols), search with icon, Run Pipeline button with spinner, category pills (max 3 + "+N"), cost badge in green, hover-reveal delete icon, empty state illustration. Report viewer (/reports/[id]): sticky action bar below nav (back arrow + Download PDF + Delete), metadata bar with date/cost/items/models, category pills, full prose article with Tailwind typography, styled prose elements (headings, links, code, blockquotes, tables). Settings (/settings): 4 grouped card sections (Provider, Models, Schedule, Categories), consistent input styling, success/error save messages. Login (/login): centered card on dark background, NavBar hidden on login page.
- What was learned: Tailwind v4's @theme inline block maps CSS custom properties to utility classes (e.g., `--color-wi-bg` → `bg-wi-bg`). Theme toggle via data attribute on `<html>` works well — inline script in `<head>` prevents FOUC. Using `bg-wi-surface/80 backdrop-blur-sm` creates a nice frosted glass effect for sticky headers. Prose styling in Tailwind v4 requires explicit class overrides for each element type (prose-headings:, prose-p:, prose-a:, etc.) when using custom color tokens.
- Codebase state: app/globals.css has full dark/light theme tokens + print CSS. app/layout.tsx has anti-flash script + NavBar. app/NavBar.tsx is new client component. All 6 pages redesigned with consistent design tokens. 19 test files, 145 tests, all green. npm run build clean. ALL epic success criteria (R1-R9) are met. Epic wi-tbw complete.
